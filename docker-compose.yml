services:
  frontend:
    build:
      context: .
      args:
        NG_APP_SERVER_HOST: ${NG_APP_SERVER_HOST:-http://localhost:3000}
    image: task-manager-frontend
    ports:
      - "${FRONTEND_PORT:-3005}:80"
    restart: unless-stopped
