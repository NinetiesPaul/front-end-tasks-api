services:
  frontend:
    build:
      context: .
      args:
        VITE_SERVER_HOST: ${VITE_SERVER_HOST:-http://localhost:3000}
    image: task-manager-frontend
    ports:
      - "${FRONTEND_PORT:-3005}:80"
    restart: unless-stopped
