services:
  frontend:
    build:
      context: .
      args:
        REACT_APP_SERVER_HOST: ${REACT_APP_SERVER_HOST:-http://localhost:8080}
    image: task-manager-frontend
    ports:
      - "${FRONTEND_PORT:-3005}:80"
    restart: unless-stopped
